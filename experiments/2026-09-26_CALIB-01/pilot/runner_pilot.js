export const meta = {
  name: 'calib-pilot',
  description: 'CALIB-01: исполняет готовые промпты (головы решают счётные задачи без инструментов) с проверкой формата и техническими повторами',
  phases: [{ title: 'Вызовы' }],
}
const ARGS = {"part": "pilot", "calls": [{"label": "P:L1D3s900", "alias": "cp00", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 637, удовлетворяют всем условиям: сумма цифр числа при делении на 4 даёт остаток 0?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L1D4s900", "alias": "cp01", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 3598, удовлетворяют всем условиям: сумма цифр числа при делении на 4 даёт остаток 2?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L1D6s900", "alias": "cp02", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 314918, удовлетворяют всем условиям: сумма цифр числа при делении на 4 даёт остаток 2?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L2D3s900", "alias": "cp03", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 767, удовлетворяют всем условиям: сумма цифр числа при делении на 5 даёт остаток 1; само число при делении на 2 даёт остаток 1?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L2D4s900", "alias": "cp04", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 1362, удовлетворяют всем условиям: сумма цифр числа при делении на 7 даёт остаток 6; само число при делении на 3 даёт остаток 0?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L3D3s900", "alias": "cp05", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 380, удовлетворяют всем условиям: сумма цифр числа при делении на 5 даёт остаток 1; само число при делении на 4 даёт остаток 0; в десятичной записи числа нет цифры 9?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L3D4s900", "alias": "cp06", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 5254, удовлетворяют всем условиям: сумма цифр числа при делении на 7 даёт остаток 0; само число при делении на 2 даёт остаток 1; в десятичной записи числа нет цифры 2?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}, {"label": "P:L2D6s900", "alias": "cp07", "kind": "answer", "prompt": "Не используй никакие инструменты и не открывай файлы: реши в уме, опираясь только на текст ниже.\n\nЗадача: Сколько целых чисел x, 1 ≤ x ≤ 200878, удовлетворяют всем условиям: сумма цифр числа при делении на 5 даёт остаток 3; само число при делении на 5 даёт остаток 3?\n\nНужен точный ответ — одно целое число. Верни: answer — целое число (только цифры); confidence — твоя вероятность того, что ответ в точности верен (от 0 до 1); reasoning — кратко, как считал (до 5 фраз)."}]}
// args: {part, calls: [{label, alias, kind: answer, prompt}]}
const now = () => { try { return new Date().toISOString() } catch (e) { return 'не контролируется' } }
const SCHEMA = { type: 'object', properties: { answer: { type: 'string', description: 'целое число, только цифры' },
  confidence: { type: 'number', minimum: 0, maximum: 1 }, reasoning: { type: 'string' } }, required: ['answer', 'confidence', 'reasoning'] }
const check = r => (/^\d+$/.test(String(r.answer).trim()) && typeof r.confidence === 'number' && r.confidence >= 0 && r.confidence <= 1) ? null : 'формат ответа'
const records = []
async function run(c) {
  for (let a = 0; a < 3; a++) {
    let res = null, err = null
    try { res = await agent(c.prompt, { label: a ? `${c.alias}#retry${a}` : c.alias, phase: 'Вызовы', schema: SCHEMA }) } catch (e) { err = String(e) }
    const problem = err || (res == null ? 'пустой ответ' : check(res))
    records.push({ part: ARGS.part, label: c.label, alias: c.alias, attempt: a, retry: a > 0, t: now(), how: 'workflow agent() со схемой', prompt: c.prompt, response: res, problem })
    if (!problem) return
  }
}
await parallel(ARGS.calls.map(c => () => run(c)))
return { part: ARGS.part, records }
