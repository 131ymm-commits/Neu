# PREREG-HIVE-01 — «Самоуправляемый улей»
Записано 26.09.2026 до прогона. План принят советом голов:
- версия 3 — большинством, 3 из 4 (`docs/council/2026-09-26_HIVE-01_session5.md`, текст — `council_decision_v3.md`);
- поправка, версия 4 — единогласно (`docs/council/2026-09-26_HIVE-01_session6.md`, текст — `council_amendment_v4.md`).

Эти два текста — полная предрегистрация. Ниже только исполнение и числа, зафиксированные до запуска. Коммит этого файла и есть заморозка.

## Откуда
Слова человека:
- «Давай составим какой-то каркас истины… законы взаимодействия агентов… и пусть эволюционирует улей».
- «Пусть они друг с другом общаются и выбирают улучшения».
- «Пусть … ставят оценки и критикуют, а другие соответственно меняются, если много критики в одно место».

## Зафиксировано до запуска
- **Вопросы.**
  - 5 поколений по 10 вопросов, зёрна 1000 + 100·g + i.
  - Отложенные: подтверждающий набор 20 вопросов (по 5 из COL3, PART, TWIN, SUB5, зерно 5000 + i) и контрольный набор 4 вопроса COLLATZ.
  - SHA-256 всех вопросов с правдой: `6d1b139b199b4cef9a100f9359179cc184e990ab630421a369190b0bded68911` (`run/freeze.json`). Правда хранится в `/root/hive_secret/`, вне репозитория и вне workflow.
- **Хеши кода:** hive_q.py, hive_core.py, hive_step.js, hive_run.py, hive_analyze.py, verify_truth.py — в `run/freeze.json`.
- **Правда вторыми способами** (`verify_truth.py`):
  - PART — разбиения на нечётные части (Эйлер), n < 300;
  - COL3 — прямой перебор, 25 графов;
  - TWIN — π₂(10^k) при k = 3…7 совпадает с опубликованными значениями; значения взяты по памяти;
  - SUB5 — прямой перебор, 10 случаев.
- **COLLATZ:** целевая величина — число шагов до первого достижения 1.
- **Исходный улей.**
  - 3 головы: аналитик, вычислитель, скептик.
  - Законы:
    1. независимые первые оценки;
    2. проверка вторым способом;
    3. честная ширина интервала;
    4. итог — медиана.
  - Пересмотр выключен, итог — медиана, личных правил нет.
- **Код-фильтр предметности:** регулярное выражение в `hive_step.js`, версия с числительными словами (поправка заседания 6).
  - Проверено: исходные законы проходят.
  - Отклоняются: «порядка трёх тысяч», «квадрат логарифма», «раскраски считай точно», «вдвое шире».
- **Мощность знакового теста при n = 20** (`power.json`, 20 000 моделирований): порог k ≥ 15.

  | Вероятность выигрыша вопроса | 0,6 | 0,7 | 0,75 | 0,8 | 0,9 |
  |---|---|---|---|---|---|
  | Мощность | 0,13 | 0,42 | 0,62 | 0,81 | 0,99 |

  Отрицательный результат означает «не обнаружено».
- **Бюджет:** потолок 200 вызовов основного прогона.
  - Худший случай поколения — 36 вызовов, резерв на итоговую проверку — 24.
  - Сухой прогон худшего случая (сгущение у всех голов, включены пересмотр и мозолистое тело): 5 поколений и итоговая проверка = 172 + 24 = 196 вызовов. Потолок достаточен.
- **Решающее правило.**
  - Фиксированная последовательность P1 → P3 → P2, каждый шаг при α = 0,05.
  - d = разность интервальных баллов по вопросу; H1: медиана d < 0; знаковый тест; ничьи и вопросы с упавшими вызовами исключаются.
  - P3 засчитывается при успехе и в основном анализе, и в пересчёте без предметных ульев. Предметный улей — тот, у которого хотя бы один закон или личное правило по слепой разметке человеком признано предметным.
- **Температура:** по умолчанию, не контролируется.

## Мои ожидания до прогона (в `journal/SELF_CALIBRATION.md`)
- **P1 (A/B со сводкой лучше F): 0,5.** Сводка показывает правду по тем же семействам. Честный выигрыш от обратной связи вероятен, но головы уже очень точны.
- **P3 (самоуправление лучше обратной связи без права менять законы, A/B против F+): 0,3.**
- **P2 (F лучше одной головы S): 0,35.** В пилоте медиана трёх голов не была лучше лучшей головы.
