# PREREG-LEVEL-04 — «Уровень под отбором»: ландшафт организаций голов и отбор in silico
Записано 26.09.2026 до фазы 2. Основание:
- решение совета, ред. 3 (council_v1.json, единогласно);
- поправка 1 (council_amend1.json, единогласно);
- замысел, калибровка и фаза 1 — DESIGN.md.

## Зафиксировано
- **Задача.** x → x² + c (mod 1000000007), L = 200. Три новые цепочки фазы 2 (зерно 97200), правда — /root/level_secret/l4_phase2.json.
- **Генотипы.** 12 (s ∈ {10, 40, 200}, v ∈ {0, 1}, r ∈ {1, 2}). Повтор двух генотипов: s200 и s200v. Всего 42 работы.
- **Потолок вызовов фазы 2** — 1000. Худший расход по фазе 0 — 906. Сухой прогон на коде — 42 работы.
- **Хеши кода (sha256, первые 16):**
```
1f23488e3feba8cc  label.py
6788218472e2facb  sim.py
ff89643526263106  org_step.js
8eec3c3c4210035e  phase0.py
e6a45ab10e69ed6d  phase0.json
```
- **Разметка исходов** — `label.py` (поправка, п. 2), вслепую к генотипу.
  - Переразметка пилота — `pilot/phase1_relabel.txt`.
  - Расхождение: в пилоте звенья не сохраняли выписанные значения, поэтому s10 размечен «отказ с догадкой», а по проверке цепочки кодом это «ошибка». С фазы 2 исполнитель сохраняет значения каждого звена.
- **Лимит на вызов (открытый вопрос совета).** Инструмент подагентов не даёт задать лимит токенов или таймаут — «не контролируется». Сбой вызова записывается в поле error и считается отказом.
- **Доля бутстреп-ландшафтов для вывода «сигнал есть» (открытый вопрос совета)** — 0,8. Фаза 3: B = 100 ландшафтов × 50 зёрен на руку.
- **Пороги** (из решения совета):
  - H1 — доля прогонов с ростом средней точности от поколения 0 к 4 в отборе ≥ 0,8 и на ≥ 0,3 выше контроля;
  - H2 — доля прогонов с ≥ 6 из 8 особей s ≤ 40 к поколению 4 в отборе на ≥ 0,3 выше контроля;
  - H3 — рост числа r = 2 от поколения 4 к 9 на ≥ 2 в отборе на ≥ 0,2 выше контроля.
- **Критерий неинформативности s200** — поправка, п. 4.
- **Основной анализ:** отказ = неверно. Вторичный — ландшафт без отказов.

## Предсказание фазы 0, записано до фазы 2
При постоянной ε ≈ 0,006 разбиение само точности не даёт: s200 ≈ s10 ≈ 0,30, помогает только дублирование. На синтетическом ландшафте этого предсказания (`sim.py synthetic`) отбор не фиксирует звенья: H2 — 0,26 в отборе против 0,51 в контроле. Фаза 1 указывает на иное: отказ длинной одиночки и ошибки на мелких звеньях. Ландшафт фазы 2 решит.

## Мои ожидания (в self_calibration)
- H1 (отбор поднимает точность) — 0,6.
- H2 (фиксируются звенья s ≤ 40) — 0,55.
- H3 (без цены растёт дублирование) — 0,35.
