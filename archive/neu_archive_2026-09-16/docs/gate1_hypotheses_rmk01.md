# Ворота 1 — ожидание ДО чтения полного текста Robichon–Monthus–Krauth, arXiv 2609.05183 (прислан Димой 2026-09-14, вечер)

Ранее (PAPERS-CHECK-2026-09d) прочитана только аннотация; ожидание по ней: теория возмущений вокруг обратимой цепи,
исключительные точки, наш ход (огрубление лифтированной цепи) отсутствует. Ниже — ожидания по полному тексту, записанные до
чтения, с тем, что можно опровергнуть текстом.

**Своя выкладка до чтения (не из статьи, из фурье-анзаца Diaconis–Holmes–Neal для лифтированного кольца).** Блок 2×2 на моде k:
λ = (1−p)cos k ± √((1−p)²cos²k − (1−2p)); пара комплексна при p ≲ k, модуль √(1−2p) ≈ 1 − p; при p ≳ k ведущая ветвь вещественна,
λ₊ ≈ 1 − k²/2p (диффузия). Минимум ρ — в исключительной точке p_EP ≈ k. Для пути со стенками первая мода k = π/L ⇒
p_EP ≈ π/L = 3.14/L, зазор в ней ≈ π/L. Наш LIFT-01b: p* = 3.0/L, c = L(1−ρ*) = 3.01 при целой сетке c — сетка не различает 3 и π.

Ожидания (E), каждое с исходом «опровергнуто текстом»:
- E1. Их параметр необратимости (какая-то доля обратных переходов/переворотов) имеет оптимум ускорения в исключительной точке
  ведущей пары; в пересчёте на нашу параметризацию p_EP·L = π ± 0.3 для пути. Опровержение: оптимум не в EP или p_EP·L вне [2.5, 3.8].
- E2. Огрубление/лумпинг лифтированной цепи (что видит описание по сайтам) — отсутствует. Опровержение: есть раздел про
  лумпинг/проекцию/наблюдаемые с собственным временем.
- E3. Времена Кемени и первого достижения через матрицу Грина; ускорение Кемени ~ L (от L² к L). Интегральное время они не
  противопоставляют лаг-1 автокорреляции описания. Опровержение: есть сравнение спектральной и корреляционной шкал наблюдаемых.
- E4. Наши точные совпадения при p = 1/L (ρ² = λ₂(mid), ρ⁴ = λ₂(σ)) — либо объясняются их формулами (низкая уверенность), либо
  не упомянуты (ожидаю второе).
- E5. Недиагонализуемость — в исключительных точках на дискретном множестве значений параметра при конечном L; для стационарных
  состояний (меандр, клин, V) картина та же качественно, смещаются p_EP. Опровержение: EP отсутствуют для непостоянных π.
- E6. Их постановка «at the onset» — малая необратимость ε → 0 вокруг обратимой цепи; наша область (окно p ≈ π/L, сильная
  необратимость на масштабе L) — у них как предел, не как фокус.

Вердикт после чтения — одной из строк: `занято` / `занято, но с другой стороны` / `не найдено` / `не наш объект — отброшено`.
Мост, который нужно проверить текстом: окно рождений LIFT-01b c ∈ [2, 3] ↔ исключительная точка p_EP = π/L (E1); если да —
предрегистрировать LIFT-01c: R(p = π/L) = π/2 и смена вещественности пары ровно в p_EP.
